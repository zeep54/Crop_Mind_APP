# Crop_Mind_APP
Crop Mind is an Android app that serves as the user-facing dashboard for an automated greenhouse irrigation and soil-monitoring system. It communicates over WiFi with a Raspberry Pi Pico 2 W, retrieving live sensor data (soil moisture, temperature) as JSON and sending pump-control commands, while all safety logic remains enforced on the hardware.
